# Local dev. Turnstile test site key below ALWAYS passes (pairs with the test
# secret in worker/.dev.vars.example). Endpoint points at `wrangler dev`.
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA
VITE_CONTACT_ENDPOINT=http://localhost:8787
